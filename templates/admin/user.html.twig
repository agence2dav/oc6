{% extends 'base.html.twig' %}

{% block title %}Bienvenue {{ app.user.username }}{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Profile</h1>
    
    <article class="card border-light mb-3">
        <div class="card-header">
            <div class="metadata"><img src="http://oc6.test/assets/avatars/{{ app.user.avatar }}" width="240px" /></div>
            <h3>{{ app.user.username | raw}}</h3>
        </div>
        <div class="card-boby">
        <div class="content">
        {% block avatar %}
            <a class="btn btn-secondary" href="{{ path("admin_avatar") }}"><i class="bi bi-person-badge"></i> Modifier son avatar</a>
        {% endblock %}
        {#% block resetPswd %}
            <a class="btn btn-secondary" href="{{ path("reset-password") }}"><i class="bi bi-person-badge"></i> Changer le mot de passe</a>
        {% endblock %#}
        </div>
        <div class="content">
            <div class="metadata">Email : {{ app.user.email }}</div>
            <a class="btn btn-secondary" href="{{ path("app_logout") }}">Déconnexion</a>
        </div>
    </article>


</div>
{% endblock %}
